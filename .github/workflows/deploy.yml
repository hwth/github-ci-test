name: Deploy

on:
  pull_request:
    types: [closed]
    branches:
      - develop

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: github.event.pull_request.merged == true
    steps:
      - name: Deploy to Heroku
        run: |
          echo 'Run deploy'
